// StandardBackendResponse defines the common structure for responses returned by the backend,
// typically used to convey status messages or error feedback.
export interface StandardBackendResponse {
  message: string;
}
